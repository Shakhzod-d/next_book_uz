"use client";

import { DefaultContext } from "@/contants/context";
import { createContext } from "react";

const SuccessfulContext = createContext(DefaultContext);

export default SuccessfulContext;
