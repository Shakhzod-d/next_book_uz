"use client";

import { DefaultContext } from "@/contants/context";
import { createContext } from "react";

const BookmarkListContext = createContext(DefaultContext);

export default BookmarkListContext;
