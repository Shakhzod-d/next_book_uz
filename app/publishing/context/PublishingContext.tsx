"use client";

import { DefaultContext } from "@/contants/context";
import { createContext } from "react";

const PublishingContext = createContext(DefaultContext);

export default PublishingContext;
